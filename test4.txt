? 
  Matthew Rinne
  test4.txt
  cs403 Programming Languages
?

#include <stdfunc.h>
#include <array.h>

main ()
{
    ? more comments ?
    var x = 5$

    for (var i=1; i<10; i++)
    {
        x++$
        x = x / i$
        call print(x)$
    }

    var str = 'cat'$
    call print(str)$

    var y = 20$

    while (y != 0)
    {
        call print(y)$
        y--$
    }

    return$
}